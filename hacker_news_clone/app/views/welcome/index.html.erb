<h3>Welcome to mini Hacker News</h3>

<div>
  <% if session[:user_id] %>
    <%= link_to 'add new post', new_post_path %>
  <% end %>
</div>

<br>

<div>
  <% @posts.each do |post| %>
    <div>
      <%=  link_to post.title, url_for(extlink(post.url)) %>
    </div>
  <% end %>
</div>
